/**
 * Defines in which mode the evitaLab should run, ultimately defines how the
 * lab will be rendered and how it will function.
 */
export enum LabRunMode {
    Standalone = 'STANDALONE',
    Driver = 'DRIVER'
}
