/**
 * Defines all supported classifier types by evitaDB.
 */
export enum ClassifierType {
    ServerName = 'serverName',
    Catalog = 'catalog',
    Entity = 'entity',
    Attribute = 'attribute',
    AssociatedData = 'associatedData',
    Reference = 'reference',
    ReferenceAttribute = 'referenceAttribute'
}
