import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes connections module. Provides connections storage, connection manager UI and public services for accessing evitaDB
 * connections.
 */
export class ConnectionsModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // todo
    }
}
