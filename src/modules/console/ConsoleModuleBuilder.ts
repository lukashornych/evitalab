import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes general console module. Provides base components and business logic for specific console implementations
 */
export class ConsoleModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // do nothing
    }
}
