import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes entity grid module. Provides entity grid UI with all the services behind it.
 */
export class EntityGridModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // todo
    }
}
