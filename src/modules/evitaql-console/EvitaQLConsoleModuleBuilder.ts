import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes evitaQL Console module. Provides evitaQL console UI.
 */
export class EvitaQLConsoleModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // todo
    }
}
