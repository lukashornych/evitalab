import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes GraphQL Console module. Provides GraphQL console UI.
 */
export class GraphQLConsoleModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // todo
    }
}
