import { ModuleBuilder } from '@/lab/ModuleBuilder'
import { LabBuilder } from '@/lab/LabBuilder'

/**
 * Initializes schema viewer module. Provides UI viewers for all schemas.
 */
export class SchemaViewerModuleBuilder implements ModuleBuilder {

    build(builder: LabBuilder): void {
        // todo
    }
}
