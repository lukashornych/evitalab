/**
 * Simplified enumeration of supported schema types
 */
export enum SchemaType {
    Catalog = 'catalog',
    Entity = 'entity',
    Attribute = 'attribute',
    AssociatedData = 'associatedData',
    Reference = 'reference'
}
